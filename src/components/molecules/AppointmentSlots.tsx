import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const AppointmentSlots = () => {
  return (
    <View>
      <Text>AppointmentSlots</Text>
    </View>
  );
};

export default AppointmentSlots;

const styles = StyleSheet.create({});
